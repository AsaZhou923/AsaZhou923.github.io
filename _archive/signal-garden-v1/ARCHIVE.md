# Signal Garden v1 — Archived

封存日期：2026-07-10

这是本次重构前站点的只读源码快照，保留原 `src/`、`public/`、`scripts/` 与构建配置，供未来追溯设计和实现决策。

该目录位于 Astro 默认构建入口之外：

- 不会生成任何页面路由；
- 不会复制到最终 `dist/`；
- 不应作为当前站点的依赖或内容来源。

当前启用的站点始终以仓库根目录的配置和 `src/`、`public/` 为准。
